{
  "name": "amplify-q",
  "version": "1.0.0",
  "description": "d",
  "main": "index.js",
  "scripts": {
    "sandbox": "npm run sandbox -w @vts/backend",
    "ssandbox": "AWS_REGION=us-east-1 npx ampx sandbox --profile ihm --dir-to-watch packages/backend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build tsconfig.json",
    "cdk": "cdk synth"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    
  },
  "devDependencies": {
    "aws-cdk": "^2.148.0",
    "aws-cdk-lib": "^2.148.0",
    "constructs": "^10.3.0",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  },  
  "workspaces"  : [
    "packages/*"
  ]
}
